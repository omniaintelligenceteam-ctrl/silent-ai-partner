# Silent AI Partner — Feature Vault

Every feature idea lives here. When we build one, move it to `built/`. When we're actively working on one, move it to `building/`.

## Folders
- `core/` — MVP features (live or almost live)
- `power/` — Power features that make us $497+/mo
- `insane/` — The stuff that makes contractors say "no way"
- `website/` — Landing page & marketing features
- `built/` — Shipped features
- `building/` — Currently in development
